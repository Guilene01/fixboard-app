apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fixboard-uploads-pvc
  namespace: fixboard
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 5Gi
